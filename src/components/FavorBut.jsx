import React from 'react';


export default function FavorBut () {
  flat.preventDefault();
  console.log('hello');
  const response = fetch(`/favorite/${el.id}`, {
    method: 'POST',
  });
  if (response.ok) {
    
  }

  return (

)
  }
